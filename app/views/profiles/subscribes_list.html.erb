<% @friends.each do |friend| %>
        
  <div class="card photo-card">
   <div class="card-body">
      
     <%= link_to friend.name, profile_path(friend.id)  %> <br>
    
   </div>
  </div>

<% end %>